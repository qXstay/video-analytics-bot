# Telegram
BOT_TOKEN=your_telegram_bot_token

# Postgres DSN (for app + scripts)
DB_DSN=postgresql://app:app@127.0.0.1:55432/video_analytics

# Optional: path to JSON (default data/videos.json)
JSON_PATH=data/videos.json